{% extends "base.html.twig" %}
{% block style %}<link href="css/style-signin.css" rel="stylesheet">
{% endblock %}
{% block title %}
  {{ parent() }}
  Consultations
{% endblock %}

{% block contenu %}
  {% if form.valide is defined %}
    {% if form.valide == true %}
      <h1 class="h3 mb-3 font-weight-normal">{{ form.message }}</h1>
    {% else %}
      <h1 class="alert alert-danger" role="alert">{{ form.message }}</h1>
    {% endif %}
  {% endif %}

  <h1>
    Consultations
  </h1>
  <div style="color:black" class="panel panel-default">
    <form class="form-signin"  action="index.php?page=consultation" method="post" enctype="application/x-www-form-urlencoded">
      <!-- Table -->
      <div class="table-responsive">
        <table style="color:black" class="table table-hover table-striped">
          <tr>
            <thead>
    <tr>
      <th scope="col">Heure</th>
      <th scope="col">Nom</th>
      <th scope="col">Prénom</th>
      <th scope="col">Date de consultation</th>
      <th scope="col">Adresse</th>
      <th scope="col">Numéro de téléphone</th>
      <th scope="col">Montant</th>
    
    </tr>
  </thead>

          
          {% for c in liste %}
            <tr> 
              <td>{{c.heureCons}}</td>
              <td>{{c.nomCli}}</td>
              <td>{{c.prenomCli}}</td>
              <td>{{c.dateCons}}</td>
              <td>{{c.adresseCli}}</td>
              <td>{{c.telCli}}</td>
              <td>{{c.montantCons}}</td>
             


     <!--        {% if session.login is defined %}
              {% if session.role == 1 %}
              <td>
                <div class="btn-group btn-group-lg">
                  <a href="index.php?page=modifc&id={{c.id}}">
                    <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a>
                </div>
              </td>
            {% endif %}
            {% endif %}

              {% if session.login is defined %}
              {% if session.role == 1 %}
              <td>
                <div class="btn-group  btn-group-lg">
                  <a href="index.php?page=gererc&id={{c.id}}">
                    <span class="glyphicon glyphicon-trash" ariahidden="true"></span></a>
                </div>
              </td>
            {% endif %}
            {% endif %}

              {% if session.login is defined %}
              {% if session.role == 1 %}
              <td><input type="checkbox" name="cocher[]" id="cocher[]" value="{{c.id}}"/></td>
            </tr>
          {% endif %}
          {% endif %} -->
          {% endfor %}
        </table>
      </div>

     <!-- {% if session.login is defined %}
      {% if session.role == 1 %}
      <button class="btn btn-lg btn-default btn-block" type="submit" name="btSupprimer">Supprimer</button>
      <a href="index.php?page=gererc" class="btn btn-lg btn-default btn-block">Gérer les livres</a>
    {% endif %}
    {% endif %} -->
    </form>
    
     <button class="btn btn-lg btn-danger btn-block" type="submit"
name="btSupprimer">Supprimer</button>
    
  </div>
{% endblock %}
